document.addEventListener('DOMContentLoaded', async () => {
  try { const r = await fetch(`${API}/api/courses`); const d = await r.json(); document.getElementById('featuredCourses').innerHTML = d.courses && d.courses.length > 0 ? d.courses.slice(0,3).map(renderCourseCard).join('') : '<p style="color:var(--text-muted);text-align:center;grid-column:1/-1;">কোনো কোর্স নেই</p>'; } catch(e) { document.getElementById('featuredCourses').innerHTML = '<p style="color:var(--text-muted);text-align:center;grid-column:1/-1;">লোড করতে সমস্যা</p>'; }
  try { const r = await fetch(`${API}/api/books`); const d = await r.json(); document.getElementById('featuredBooks').innerHTML = d.books && d.books.length > 0 ? d.books.slice(0,3).map(renderBookCard).join('') : '<p style="color:var(--text-muted);text-align:center;grid-column:1/-1;">কোনো বই নেই</p>'; } catch(e) { document.getElementById('featuredBooks').innerHTML = '<p style="color:var(--text-muted);text-align:center;grid-column:1/-1;">লোড করতে সমস্যা</p>'; }
});
